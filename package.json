{
  "name": "github-cicd-plugin",
  "version": "1.0.0",
  "description": "GitHub Action to extract URLs from source code and POST them to a server",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build index.js -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "axios": "^1.6.0",
    "fast-glob": "^3.3.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
